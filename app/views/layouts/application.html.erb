<!DOCTYPE html>
<html>
  <head>
  <%= favicon_link_tag 'favicon.ico' %>
  <link href="https://fonts.googleapis.com/css?family=Lato:100,100i,300,300i,400,400i,500,600,700,700i,900,900i" rel="stylesheet">
    <title><%= yield(:title) %> | Homework Hotline</title>
    <%= csrf_meta_tags %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= action_cable_meta_tag %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <% if current_user %>
  <div class="container">
  <div class="infoparent">
  <%= image_tag "hhcircle.png", style: "width:60px; display:inline-block" %><h1 style="display: inline-block; margin-left: 10px; position: relative;bottom: 18px;font-size: 30px;"><%= yield(:page) %></h1>
<div class="name"><%= current_user.firstname.capitalize%> <%= current_user.lastname.capitalize%></div>
<div class="title"><%= current_user.role.to_s.tr('_', ' ').upcase %></div>
</div></div><br><br>
<% end %>
    <%= yield %>
    <style type="text/css">
    @font-face {
  font-family: 'icomoon';
  src: url('/assets/icomoon.eot');
  src: url('/assets/icomoon.eot') format("embedded-opentype"),
    url('/assets/icomoon.ttf') format("truetype"),
    url('/assets/icomoon.woff') format("woff"),
    url('/assets/icomoon.svg#icofonts') format("svg");
  font-weight: normal;
  font-style: normal;
}
    [class^="icon-"], [class*=" icon-"] {
      /* use !important to prevent issues with browser extensions that change fonts */
      font-family: 'icomoon' !important;
      speak: none;
      font-style: normal;
      font-weight: normal;
      font-variant: normal;
      text-transform: none;
      line-height: 1;

      /* Better Font Rendering =========== */
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }


.icon-alert:before {
  content: "\1f32b";
}

.icon-up-arrow:before {
  content: "\e900";
  color: #65ee95;
}

.icon-down-arrow:before {
  content: "\e902";
  color: #e64759;
}

.icon-equal-dash:before {
  content: "\e901";
  color: #999;
}
      a {
  color:#00b7ff !important;
  text-decoration: none !important;
}
a:hover {
  background-color: transparent !important;
  color: #0089bf !important;
}
p,h1,h2,h3,h4,h5,h6,li,a {
  font-family: 'Lato', sans-serif;
}
.footer {
  position: absolute;
  bottom: 0;
  box-sizing: border-box;
  width: 100%;
  background-color: #f2f2f2;
  padding: 15px 16px;
  font-size: 16px;
  color: #ccc;
  text-align:center;
  font-family: Lato;
  font-weight: 500;
  letter-spacing: 3px;
  margin-left: -80px;
}
.infoparent {
  width: 934px;
  margin-bottom: 10px;
}
html {
    position: relative;
    min-height: 100%;
}
body {
    margin-bottom: 48px;
    margin-left: 80px; /* bottom = footer height */
}
.name {
  width: 300px;
  background:#65ef96;
  font-size: 30px;
  font-family: Lato;
  font-weight: 600;
  padding: 15px 0;
  padding-left: 20px;
  text-align:left;
  letter-spacing: 1px;
  position: relative;
  top: 20px;
}
    .container {
      display: flex;
      justify-content: center;
      margin-right: 80px;
    }
.title {
  width: 180px;
  background:#f2f2f2;
  font-size: 13.5px;
  font-family: Lato;
  font-weight: 500;
  padding: 5px 0;
  text-align:center;
  letter-spacing: 1px;
  position: relative;
  top: 20px;
}
  .adminbar {
      width: 934px;
      height: 40px;
      z-index: 100;
      background: #f2f2f2;
      margin-bottom: 30px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .adminbar > a {
      float: left;
    display: block;
    color: #333 !important;
    text-align: center;
    font-weight: 500;
    letter-spacing: 2px;
    padding: 10px 0px;
    width: 233.5px;
    text-decoration: none;
    font-size: 20px;
    border-top: 3px solid #fff;
    border-bottom: 3px solid #fff;
    }
    .adminbar > a:hover {
      border-bottom:3px solid #65ef96;
      color: #333 !important;
    }
    .adminbar > a.active {
      border-bottom: 3px solid #65ef96;
    }
  .three {
    width: 20px;
    height: 20px;
    background-color: #65ef96;
    -webkit-animation:spin 8s ease-in-out infinite;
        -moz-animation:spin 8s ease-in-out infinite;
      animation:spin 8s ease-in-out infinite;
  }
  .two {
    width: 40px;
    height: 40px;
    background-color: white;
    display: flex;
    justify-content: center;
    align-items: center;
    -webkit-animation:spin 4s ease-in-out infinite;
        -moz-animation:spin 4s ease-in-out infinite;
      animation:spin 4s ease-in-out infinite;
  }
  @-moz-keyframes back { 100% { -moz-transform: rotate(-360deg); } }
@-webkit-keyframes back { 100% { -webkit-transform: rotate(-360deg); } }
@keyframes back { 100% { -webkit-transform: rotate(-360deg); transform:rotate(-360deg); } }
  .one {
    width: 60px;
    height: 60px;
    background-color: #65ef96;
    display: flex;
    justify-content: center;
    align-items: center;
    position: absolute;
    top:0;
    bottom: 0;
    left: 0;
    right: 0;
    margin: auto;
    -webkit-animation:spin 2s ease-in-out infinite;
        -moz-animation:spin 2s ease-in-out infinite;
      animation:spin 2s ease-in-out infinite;
}
@-moz-keyframes spin { 100% { -moz-transform: rotate(360deg); } }
@-webkit-keyframes spin { 100% { -webkit-transform: rotate(360deg); } }
@keyframes spin { 100% { -webkit-transform: rotate(360deg); transform:rotate(360deg); } }

</style>
    <div class="footer">(C) 2017 HOMEWORK HOTLINE</div>
  </body>
</html>
